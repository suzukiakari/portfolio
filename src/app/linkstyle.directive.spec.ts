import { LinkstyleDirective } from './linkstyle.directive';

describe('LinkstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkstyleDirective();
    expect(directive).toBeTruthy();
  });
});
